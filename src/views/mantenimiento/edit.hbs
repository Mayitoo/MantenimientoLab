<section class="section">
        <div class="container p-4">
    <div class="row">
        <div class="col-md-4">
            <div class="card">
                <div class="card-header">
                    <h3>Mantenimiento</h3>

                </div>
                <div class="card-body">
                    <form action="/mantenimiento/edit/{{mantenimiento.id_mantenimiento}}" method="POST" >
                        <div class="form-group">
                            <select class="form-control" name="fk_ticket">
                                <option value="{{mantenimiento.fk_ticket}}">{{mantenimiento.fk_ticket}}</option>
                                {{#each soporte}}
                                <option value="{{fk_ticket}}" name="fk_ticket">{{fk_ticket}}</option>
                                {{/each}}
                                </select>
                        </div>
                        <div class="form-group">
                            <input type="text" class="form-control" name="descripcion" value="{{mantenimiento.descripcion}}">
                        </div>
                        <div class="form-group">
                            <select class="form-control" name="estatus">
                                <option value="{{mantenimiento.estatus}}">{{mantenimiento.estatus}}</option>
                                <option value="1" name="status">Funcionando</option>
                                <option value="2" name="status">En reparación</option>
                                </select>
                        </div>
                        <div class="form-group">
                            <button class="btn btn-success btn-block">
                                Guardar
                            </button>
                            
                        </div>
                    </form>
                 </div>
            </div>
        </div>
    </div>

</div>
</section>